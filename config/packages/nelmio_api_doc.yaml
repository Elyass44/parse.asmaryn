nelmio_api_doc:
    documentation:
        info:
            title: Parse — Resume Parser API
            description: Upload PDF résumés and extract structured data via Mistral AI.
            version: 1.0.0
    areas:
        path_patterns:
            - ^/api(?!/doc$) # Documents /api/* except /api/doc itself
